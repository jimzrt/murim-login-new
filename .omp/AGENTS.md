# Project Context

This repository uses the deterministic chapter controller described in the
root `AGENTS.md`. Begin and resume every requested chapter by running
`python tools/workflow.py status N`; execute only its reported next action.
